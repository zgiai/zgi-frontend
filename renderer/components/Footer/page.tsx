import React from 'react';

const Footer = () => {
    return (
        <footer className="bg-white border-t border-gray-200 p-4 text-center text-sm text-gray-500">
            ZGIChat is open source and your data is stored locally.
        </footer>
    );
};

export default Footer;